# Vendored third-party libraries are shipped verbatim: never normalise or
# convert their line endings, so a checkout on any platform is byte-identical
# to what upstream published.
hrms_dashboard/static/lib/** -text
